import Slam
import Quick
import Nimble

class FooSpec: QuickSpec {
    
    override func spec() {
        it("is another test") {
            let foo = Foo()
            expect(1).to(equal(1))
        }
    }
    
}
