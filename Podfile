# Uncomment this line to define a global platform for your project
# platform :ios, '6.0'

target "Slam" do
  pod "Google/SignIn"
end

target "SlamTests" do
  use_frameworks!
  pod "Quick", "~> 0.3.0"
  pod "Nimble"
end
